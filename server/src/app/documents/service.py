"""Headless Documents service. Every public function takes
`workspace_uuid` + `caller_member_uuid` keyword-only and returns Pydantic
models (a2ui-strategy.md).

Land alongside the schemas in the next step.
"""

from __future__ import annotations

# Service functions land here as the Documents surface grows.
